# 脉冲计算基质机制不变量

[英文版本](ESSENCE.md)

状态：`0.2.0-alpha.1` 公开机制边界。

这些不变量描述实现必须保持的性质，不是意识、自主生活或长期因果性的主张。

## 世界与身份

1. 对同一个数据库和 fencing epoch，一台主机只能有一个活跃 `PulseWorld` 所有者；
2. Engram 不等于浏览器标签页、任务、进程或模型上下文；
3. 每个 Engram generation 拥有一个持久 Pi 会话；会话连续性与进程寿命不同；
4. 换代可以创建新的 generation 与会话，但必须保留 lineage 和对上一代的因果引用。

## 时间与计算

5. 调度器时标只推进有界世界机制，不证明 Engram 已经思考；
6. `Pulse` 是世界层资源分配；准入模型计算时，完整 Pi turn 必须稳定后才发布结果；
7. 模型—工具往返不能切在工具调用与工具结果之间；
8. 不同 Engram 之间的并发有上限，同一 Engram 内部保持串行。

## 信息边界

9. 内容流携带最终自然语言，不携带工具信封或完整 transcript；
10. 频谱流改变时机与强度，不改变内容或权限；
11. 隧道流通过既有目标会话携带定向自然语言请求与结果，不创建临时身份；
12. 结构化 provider 与工具轨迹留在原会话内部。

## 持久化与恢复

13. Pi JSONL 是模型—工具会话真源；SQLite 是世界状态、关系、租约、因果终态与恢复真源；
14. 可能已经发生但无法确认的外部工作进入 `uncertain`，重启后不自动重放；
15. 旧所有者或旧 fencing epoch 不能提交新的世界状态；
16. 关闭必须先停止准入，再释放所有权与存储。

## 授权

17. 能力 Profile、启动 token、精确浏览器 Origin、允许清单、审批、RoleLease 与
    TaskRelationship 是彼此独立的边界；
18. Purpose 文本、模型输出或界面标签不能授予能力；
19. 默认 Profile 必须拒绝 HTTP 状态修改。

## 学习状态

20. 连接、委派与屏状体学习路径在数值和语义上保持分离；
21. factory 权重是版本化基线，field 权重是可回滚的本地覆盖；
22. 学习得到的数值状态不能改写 provider 模型参数或会话 transcript。

## 证据

23. 代码存在是 E0，确定性合同是 E1，经授权外部观察是 E2，可复现发布流程是 E3，
    长期反事实因果性是 E4；
24. UI、本机 TCP、SQLite、模拟测试与操作系统回归仍只属于 E1；
25. 低等级证据不能被表述为高等级证据。

参见[架构](ARCHITECTURE.zh-CN.md)、
[能力说明](docs/CAPABILITIES.zh-CN.md)与
[能力证据](docs/evidence/README.zh-CN.md)。
